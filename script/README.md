# script

Utility scripts for build and smoke testing.

- `build.ts`: runs `npm run build:client` and `npm run build:server` in sequence.
- `test-analyze.js`: posts a sample analyze request to a running server.
