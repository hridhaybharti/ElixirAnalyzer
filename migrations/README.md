# migrations

Database migrations generated by Drizzle.

- `0000_*.sql`, `0001_*.sql`: schema changes.
- `meta/`: snapshots and `_journal.json` used by drizzle-kit.

Apply the latest schema with `npm run db:push` (uses `drizzle.config.ts` and `DATABASE_URL`).
