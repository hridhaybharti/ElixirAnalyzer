# tests

Pytest suite for the legacy Python backend.

Covers:
- heuristics scoring and signal generation
- validators and input detection
- SQLite persistence behavior
- WHOIS adapter behavior

Run with `pytest` after installing `requirements.txt`.
